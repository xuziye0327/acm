#include <cstdio>

const int maxn = 200 + 7;

int a[maxn];
int d[maxn][maxn][maxn];

int dp(int l, int r, int k) {

}

int main () {
    int T, Case(0);
    scanf("%d", &T);
    while (T--) {
        int n;
        scanf("%d", &n);
        for (int i = 0; i < n; ++i) scanf("%d", &a[i]);

    }
    return 0;
}
